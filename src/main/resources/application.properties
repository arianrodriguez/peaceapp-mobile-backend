spring.application.name=peaceapp

# Spring DataSource Configuration
spring.datasource.url=jdbc:mysql://innovatech.mysql.database.azure.com/peaceapp?useSSL=true&requireSSL=true&serverTimezone=UTC
spring.datasource.username=innovatech
spring.datasource.password=UPC2024#
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Spring Data JPA Configuration
spring.jpa.show-sql=true

#Spring Data JPA Hibernate Configuration
spring.jpa.open-in-view=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
# Disable Hibernate usage of JDBC metadata
spring.jpa.properties.hibernate.boot.allow_jdbc_metadata_access=false

# Database initialization should typically be performed outside of Spring lifecycle
spring.jpa.hibernate.ddl-auto=none
spring.sql.init.mode=never

